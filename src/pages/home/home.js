import { Box } from "@mui/material";
import { styled } from "@mui/system";

const StyledWrapper = styled(Box)(({ theme }) => ({

}))

const Home = () => {
	return (
		<StyledWrapper>
			<div>Home</div>
		</StyledWrapper>
	)
}

export default Home