import { Box } from "@mui/material";
import { styled } from "@mui/system";

const StyledWrapper = styled(Box)(({ theme }) => ({

}))

const Why = () => {
	return (
		<StyledWrapper>
			<div>Why</div>
		</StyledWrapper>
	)
}

export default Why